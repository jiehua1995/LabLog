{
  "name": "LabLog",
  "version": "0.0.2",
  "author": "jiehua",
  "description": "  ",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "npx tailwindcss -i ./renderer/styles.css -o ./renderer/tailwind.css",
    "start": "npm run build-css && electron .",
    "build": "npm run build-css && electron-builder"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "electron": "^32.0.1",
    "electron-builder": "^24.13.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jiehua1995/LabLog"
  }
}
